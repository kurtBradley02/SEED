name: 03 DB Test

on:
  workflow_dispatch:
  workflow_call:

jobs:
  db-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_pass
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install pgTAP and pg_prove
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-16-pgtap libtap-parser-sourcehandler-pgtap-perl

      - name: Enable pgTAP extension
        run: |
          PGPASSWORD=test_pass psql -h localhost -U test_user -d test_db \
            -c "CREATE EXTENSION IF NOT EXISTS pgtap;"

      - name: Build database state
        run: |
          for f in ./01_Database/*.sql; do
            echo "Running $f"
            PGPASSWORD=test_pass psql -h localhost -U test_user -d test_db -f "$f"
          done

      - name: Run DB tests
        run: |
          pg_prove -h localhost -U test_user -d test_db ./01_Database/test_operations.sql
        env:
          PGPASSWORD: test_pass