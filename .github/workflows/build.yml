name: Build

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  db:
    uses: ./.github/workflows/db_pipe.yml

  api:
    needs: db
    uses: ./.github/workflows/api_pipe.yml

  ui:
    needs: api
    uses: ./.github/workflows/ui_pipe.yml
